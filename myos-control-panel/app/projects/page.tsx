'use client'
import {useEffect,useState} from 'react'
export default function Projects(){const [items,setItems]=useState<any[]>([]),[name,setName]=useState(''),[type,setType]=useState('Residential'),[saving,setSaving]=useState(false)
async function load(){const r=await fetch('/api/myos/projects');const d=await r.json();setItems(d.projects??[])} useEffect(()=>{load()},[])
async function add(){if(!name.trim())return;setSaving(true);await fetch('/api/myos/projects',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,type,status:'active'})});setName('');await load();setSaving(false)}
return <main style={{maxWidth:900,margin:'40px auto',padding:24}}><p>MYOS / PROJECTS</p><h1>Projects OS</h1><p>Turn real architectural work into execution, learning, portfolio and content opportunities.</p><div style={{display:'flex',gap:8,margin:'24px 0'}}><input placeholder="Project name" value={name} onChange={e=>setName(e.target.value)}/><select value={type} onChange={e=>setType(e.target.value)}><option>Residential</option><option>Villa</option><option>Commercial</option><option>Interior</option><option>Shop Drawing</option><option>Finishing</option></select><button onClick={add} disabled={saving}>{saving?'Adding…':'Add Project'}</button></div>{items.map(p=><article key={p.id} style={{padding:18,margin:'10px 0',border:'1px solid #ddd',borderRadius:12}}><b>{p.name}</b><div>{p.type||'Project'} · {p.status}</div><small>{p.description||'Ready to connect tasks, portfolio assets and content.'}</small></article>)}</main>}
