import {NextResponse} from 'next/server'
import {createClient} from '@/lib/supabase/server'
const defaults:any={residential:['Project Setup','Drawing Register','BOQ','Quantity Takeoff','Material Schedule','Procurement','Daily Site Report','Inspection & QAQC','Progress','Cost Control','Snag List','Handover'],commercial:['Project Setup','Drawing Register','BOQ','Quantity Takeoff','Material Schedule','Procurement','Subcontractors','Daily Site Report','QAQC','Progress','Cost Control','Snag List','Handover'],finishing:['Project Setup','Existing Conditions','Finishes Schedule','BOQ','Quantity Takeoff','Material Approvals','Procurement','Daily Site Report','Trade Progress','Inspection','Snag List','Cost Control','Handover']}
export async function POST(req:Request){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});const b=await req.json();if(!b.project_id)return NextResponse.json({error:'project_id required'},{status:400});const type=String(b.project_type||'residential').toLowerCase();const sheets=defaults[type]||defaults.residential;const {data,error}=await s.from('ai_recommendations').insert({user_id:user.id,project_id:b.project_id,recommendation_type:'workspace_setup',title:'Project workbook specification',recommendation:'Generated standardized workbook specification.',priority:'high',confidence:.92,action:'Create workbook sheets from this specification.',source_refs:{project_type:type,sheets},status:'new'}).select().single();if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({recommendation_id:data.id,project_type:type,sheets})}
