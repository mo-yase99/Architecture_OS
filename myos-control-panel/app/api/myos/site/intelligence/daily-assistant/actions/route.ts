import {NextResponse} from 'next/server'
import {createClient} from '@/lib/supabase/server'
export async function POST(req:Request){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});const b=await req.json();if(!b.project_id||!Array.isArray(b.actions))return NextResponse.json({error:'project_id and actions required'},{status:400});const rows=b.actions.map((a:any)=>({project_id:b.project_id,title:String(a.title||a),status:'Not Started',priority:String(a.priority||'normal'),due_date:a.due_date||null,notes:String(a.notes||'Generated by Daily Engineering Assistant')}));const {data,error}=await s.from('tasks').insert(rows).select();if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({created:data?.length||0,tasks:data||[]})}
