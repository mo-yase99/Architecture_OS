import {NextResponse} from 'next/server'
import {createClient} from '@/lib/supabase/server'
const stages:any={residential:['Project Setup','Site Survey','Concept Design','Detailed Design','BOQ & Cost Plan','Procurement','Execution','Inspection & Snagging','Handover'],commercial:['Project Setup','Site Survey','Concept & Coordination','Detailed Design','Authority Coordination','BOQ & Procurement','Execution','QA/QC','Handover'],finishing:['Site Survey & Measurement','Finishes Schedule','Shop Drawings','BOQ & Takeoff','Material Approvals','Procurement','Execution by Trade','Inspection & Snagging','Handover']}
export async function POST(req:Request){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});const b=await req.json();if(!b.project_id)return NextResponse.json({error:'project_id required'},{status:400});const type=String(b.project_type||'residential').toLowerCase();const names=stages[type]||stages.residential;const durations=[1,3,7,14,3,7,30,7,3];const {data:existing}=await s.from('tasks').select('title').eq('project_id',b.project_id);const have=new Set((existing||[]).map((x:any)=>x.title));const rows=names.filter((n:string)=>!have.has(n)).map((n:string,i:number)=>({user_id:user.id,project_id:b.project_id,title:n,status:'todo',priority:i<2?'high':'medium',due_date:new Date(Date.now()+durations.slice(0,i+1).reduce((a:number,x:number)=>a+x,0)*86400000).toISOString().slice(0,10),description:`Standard MYOS project stage: ${n}`}));if(rows.length)await s.from('tasks').insert(rows);return NextResponse.json({created:rows.length,stages:names,total_estimated_days:durations.reduce((a:number,x:number)=>a+x,0)})}
