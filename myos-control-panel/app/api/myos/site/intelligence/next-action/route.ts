import {NextResponse} from 'next/server'
import {createClient} from '@/lib/supabase/server'
export async function GET(req:Request){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});const project_id=new URL(req.url).searchParams.get('project_id');if(!project_id)return NextResponse.json({error:'project_id required'},{status:400});const [{data:tasks},{data:jobs},{data:recs}]=await Promise.all([s.from('tasks').select('*').eq('project_id',project_id).neq('status','Done').order('due_date',{ascending:true}).limit(10),s.from('file_processing_jobs').select('*').eq('project_id',project_id).order('created_at',{ascending:false}).limit(5),s.from('ai_recommendations').select('*').eq('project_id',project_id).eq('status','new').order('created_at',{ascending:false}).limit(10)]);const actions:any[]=[];for(const t of tasks||[])actions.push({kind:'task',priority:t.priority,title:t.title,due_date:t.due_date,reason:'Open project task'});for(const r of recs||[])actions.push({kind:'ai',priority:r.priority,title:r.title,action:r.action,reason:'New AI recommendation'});for(const j of jobs||[])if(['queued','processing'].includes(j.status))actions.push({kind:'processing',priority:'high',title:`Processing: ${j.file_name}`,progress:j.progress,reason:'File intelligence still running'});return NextResponse.json({next_action:actions[0]||{kind:'none',title:'No pending action',reason:'Project is currently clear'},actions})}
