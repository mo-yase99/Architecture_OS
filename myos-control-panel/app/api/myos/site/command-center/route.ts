import { NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'
export async function GET(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:'Unauthorized'},{status:401});const [{data:projects},{data:reports},{data:issues},{data:files}]=await Promise.all([s.from('projects').select('id,name,project_code,project_type,status,location').eq('owner_id',user.id).order('updated_at',{ascending:false}),s.from('site_daily_reports').select('*').eq('owner_id',user.id).order('report_date',{ascending:false}).limit(7),s.from('site_issues').select('*').eq('owner_id',user.id).neq('status','closed').order('created_at',{ascending:false}).limit(20),s.from('project_file_assets').select('id,project_id,name,file_type,discipline,document_type,revision,status,last_modified_at').eq('owner_id',user.id).order('updated_at',{ascending:false}).limit(50)]);return NextResponse.json({project:projects?.[0]??null,reports:reports??[],issues:issues??[],files:files??[],counts:{projects:projects?.length??0,openIssues:issues?.length??0,documents:files?.length??0}})}
