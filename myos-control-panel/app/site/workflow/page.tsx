'use client'
import {useEffect,useState} from 'react'
export default function Workflow(){const[d,setD]=useState<any>(null);useEffect(()=>{fetch('/api/myos/site/command-center').then(r=>r.json()).then(setD)},[]);return <main style={{maxWidth:1200,margin:'40px auto',padding:24}}><p>MYOS / SITE OS / WORKFLOW</p><h1>Project Workflow</h1><p>Capture once. Connect everything.</p><div className="card" style={{marginTop:24}}><h2>Daily execution chain</h2><div style={{display:'grid',gridTemplateColumns:'repeat(7,1fr)',gap:8}}>{['Daily Report','Activity','Quantity','BOQ','Material','Procurement','Cost'].map((x,i)=><div className="card" key={x}><strong>{i+1}</strong><p>{x}</p></div>)}</div></div><div className="card" style={{marginTop:16}}><h2>Technical chain</h2><div style={{display:'grid',gridTemplateColumns:'repeat(5,1fr)',gap:8}}>{['Drawing','Revision','Change Detection','Issue','RFI'].map((x,i)=><div className="card" key={x}><strong>{i+1}</strong><p>{x}</p></div>)}</div></div><div className="card" style={{marginTop:16}}><h2>Current project signal</h2><p>{d?.project?.name??'No active project'} · {d?.counts?.documents??0} files · {d?.counts?.openIssues??0} open issues · {d?.counts?.openRfis??0} open RFIs · {d?.counts?.pendingProcurement??0} procurement items</p></div></main>}
