'use server'
import {createClient} from '@/lib/supabase/server'
export async function createSiteIssue(form:FormData){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)throw new Error('Unauthorized');const {error}=await s.from('site_issues').insert({owner_id:user.id,project_id:String(form.get('project_id')),title:String(form.get('title')),description:String(form.get('description')||''),severity:String(form.get('severity')||'medium'),status:'open',rfi_required:form.get('rfi_required')==='on'});if(error)throw new Error(error.message)}
export async function createProgressLog(form:FormData){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)throw new Error('Unauthorized');const {error}=await s.from('project_progress_logs').insert({user_id:user.id,project_id:String(form.get('project_id')),log_date:String(form.get('log_date')),activity:String(form.get('activity')),quantity:Number(form.get('quantity')||0),unit:String(form.get('unit')||''),percent_complete:Number(form.get('percent_complete')||0),notes:String(form.get('notes')||'')});if(error)throw new Error(error.message)}
export async function createProcurementRequest(form:FormData){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)throw new Error('Unauthorized');const {error}=await s.from('project_procurement').insert({user_id:user.id,project_id:String(form.get('project_id')),item_name:String(form.get('item_name')),quantity:Number(form.get('quantity')||0),unit:String(form.get('unit')||''),supplier:String(form.get('supplier')||''),required_at:String(form.get('required_at')||'')||null,status:'requested'});if(error)throw new Error(error.message)}
